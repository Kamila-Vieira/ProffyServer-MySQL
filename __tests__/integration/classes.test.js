const request = require('supertest');
const app = require('../../src/app');
const truncate = require('../utils/truncate');
const { Class } = require('../../src/app/models')

describe('GiveClasses', () =>{

    it('should create a subject', async () => {
        /* const clsses = await Class.create('Class', {
            subject: 'Matemática',
            cost: 120.00
          }); */
        //expect(clsses).toBe();
    });
});